/*
  @ Prerequisites
      $ HTML, CSS & JS

      $ Basics of Typescript

  @ Development Environment
    $ Node / NPM
      = `nodejs.org`
      = 'node -v'
      = 'npm -v'

    $ Angular CLI
      = `npm install -g @angular/cli`
      = 'ng version'

    $ Text Editor of choice (I use VS Code)
*/
